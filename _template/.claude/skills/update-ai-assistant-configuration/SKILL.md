---
name: update-ai-assistant-configuration
description: 'Update AI assistant configuration files by fetching and running the latest auto-generate script from the central repository.'
user-invocable: true
---
<!--

  Auto-generated by https://github.com/dsb-infra/.github-private — do not edit manually.
  To re-generate: echo 'auto' | bash <(gh api -H "Accept: application/vnd.github.v3.raw" /repos/dsb-infra/.github-private/contents/github-copilot-files/auto-generate/update-files.sh);

-->
<!--

  Read by: Claude Code (as /skill-name slash command), VS Code Copilot (as /skill-name slash command).

-->
# Update AI assistant configuration

Run the central auto-generate script to update AI assistant configuration files (Claude Code instructions, skills, Copilot prompts, etc.) in the current repository.

## Steps

1. Verify that `gh` (GitHub CLI) is installed and the user is authenticated. If not, inform the user and stop.
2. Run the following command from the repository root directory:
   ```
   echo 'auto' | bash <(gh api -H "Accept: application/vnd.github.v3.raw" /repos/dsb-infra/.github-private/contents/github-copilot-files/auto-generate/update-files.sh)
   ```
3. Show the full output of the script to the user.
4. If the script fails, inform the user of the error and suggest corrective actions.
5. If the script succeeds, briefly summarize which files were created or updated.
